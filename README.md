# NodeAWS

### Install Node js on AWS server

`sudo apt update`
`sudo apt install nodejs`
`sudo apt install npm`
`npm i`

### Install Node packages

`npm i express`

### Run API on AWS

`node index.js`
